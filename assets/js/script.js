jQuery(document).ready(function ($) {
    // Determine if we need to do anything specific for FlexSlider reset
    // Sometimes adding elements dynamically might require triggering a resize
    // But since we render on server side, it should be fine.
});
